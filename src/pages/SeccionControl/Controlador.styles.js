import { StyleSheet } from "react-native";
// Estilos
export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    padding: 20,
    backgroundColor: "rgb(0,0,0)",
  },
  button: {
    marginVertical: 10,
    width: "80%",
    borderRadius: 20,
  },
});
